# Lesson 08 — Коллекции (Collections)

## 🧩 Что такое коллекции
Коллекции — это **гибкая замена массивам**.  
Они позволяют хранить, изменять и обрабатывать наборы данных любого размера без заранее фиксированной длины.

Главный пакет — `java.util`.

---

## 🔹 Почему не массивы?
Массив (`int[]`) имеет фиксированную длину.  
Если нужно добавить элемент — создавай новый массив.  
Коллекции же динамические: размер меняется автоматически.

---

## 🔹 Иерархия коллекций

```
Collection
 ├── List
 │    ├── ArrayList
 │    └── LinkedList
 ├── Set
 │    ├── HashSet
 │    └── LinkedHashSet
 └── Map
      ├── HashMap
      └── TreeMap
```

---

## 🔹 List — список элементов (с индексами)

```java
import java.util.*;

public class ExampleList {
    public static void main(String[] args) {
        List<String> names = new ArrayList<>();

        names.add("Sasha");
        names.add("Alina");
        names.add("Igor");

        System.out.println(names); // [Sasha, Alina, Igor]
        System.out.println("Размер списка: " + names.size());

        names.remove("Igor");
        System.out.println(names.contains("Alina")); // true

        for (String n : names) {
            System.out.println("Имя: " + n);
        }
    }
}
```

---

## 🔹 Set — множество (уникальные элементы)

```java
import java.util.*;

public class ExampleSet {
    public static void main(String[] args) {
        Set<String> tags = new HashSet<>();

        tags.add("qa");
        tags.add("java");
        tags.add("qa"); // дубликат — не добавится

        System.out.println(tags); // [qa, java]
        System.out.println("Размер множества: " + tags.size());
    }
}
```

`Set` полезен, если важно **избавиться от дублей**.

---

## 🔹 Map — пары ключ–значение

```java
import java.util.*;

public class ExampleMap {
    public static void main(String[] args) {
        Map<String, Integer> ages = new HashMap<>();

        ages.put("Sasha", 32);
        ages.put("Alina", 28);
        ages.put("Igor", 30);

        System.out.println(ages);
        System.out.println("Возраст Саши: " + ages.get("Sasha"));

        ages.remove("Igor");
        System.out.println("Содержит ключ Alina? " + ages.containsKey("Alina"));
    }
}
```

`Map` — это как словарь: у каждого ключа есть одно значение.

---

## 🔹 Обход коллекций

**for-each**:
```java
for (String name : names) {
    System.out.println(name);
}
```

**Iterator**:
```java
Iterator<String> it = names.iterator();
while (it.hasNext()) {
    System.out.println(it.next());
}
```

---

## 🔹 Часто используемые методы

| Метод | Описание |
|-------|-----------|
| `add(E e)` | добавить элемент |
| `remove(Object o)` | удалить элемент |
| `clear()` | очистить коллекцию |
| `contains(Object o)` | проверить наличие |
| `size()` | количество элементов |
| `isEmpty()` | проверить, пустая ли коллекция |

---

## 🔹 Отличия List / Set / Map

| Тип | Уникальность | Порядок | Доступ |
|------|---------------|----------|---------|
| `List` | допускает дубликаты | сохраняет порядок | по индексу |
| `Set` | уникальные элементы | порядок не гарантирован | по итерации |
| `Map` | уникальные ключи | порядок не гарантирован | по ключу |

---

## 🔹 Применение коллекций в AQA

Коллекции в тестировании встречаются **на каждом шаге**:

### 🧠 1. Сравнение списков API-ответов
```java
List<String> expected = List.of("USD", "EUR", "CNY");
List<String> actual = apiResponse.getCurrencies();

assertTrue(actual.containsAll(expected));
```

### 🧠 2. Проверка уникальности
```java
Set<String> ids = new HashSet<>(apiResponse.getIds());
assertEquals(ids.size(), apiResponse.getIds().size());
```

### 🧠 3. Хранение пар данных
```java
Map<String, String> credentials = new HashMap<>();
credentials.put("login", "admin");
credentials.put("password", "1234");

assertEquals("admin", credentials.get("login"));
```

---

## ✅ После этого урока ты должен уметь:

- Использовать `List`, `Set`, `Map`
- Добавлять, удалять и проверять элементы
- Перебирать коллекции с помощью `for-each` и `Iterator`
- Применять коллекции для хранения тестовых данных и проверки ответов API
