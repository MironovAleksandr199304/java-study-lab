# 🧠 Шпаргалка по лямбдам и Stream API

## Что такое лямбда
Лямбда — это короткая форма записи анонимной функции.  
Она позволяет передать поведение как аргумент, не создавая отдельный класс или метод.

### Пример:
```java
n -> n * n
```
Эквивалентно:
```java
new Function<Integer, Integer>() {
    public Integer apply(Integer n) { return n * n; }
}
```

---

## Основные методы Stream API и их смысл

### 1. filter
Отбирает элементы по условию.

```java
nums.stream()
    .filter(n -> n > 0)
    .forEach(System.out::println);
```
> Пропускает только положительные числа.

---

### 2. map
Преобразует каждый элемент.

```java
names.stream()
    .map(String::toUpperCase)
    .forEach(System.out::println);
```
> Переводит все строки в верхний регистр.

---

### 3. sorted
Сортирует элементы (по умолчанию — по возрастанию).

```java
nums.stream()
    .sorted()
    .forEach(System.out::println);
```
> Можно задать свой компаратор: `.sorted(Comparator.reverseOrder())`.

---

### 4. distinct
Удаляет дубликаты.

```java
nums.stream()
    .distinct()
    .forEach(System.out::println);
```

---

### 5. collect
Собирает элементы потока обратно в коллекцию.

```java
List<Integer> list = nums.stream()
    .filter(n -> n > 0)
    .collect(Collectors.toList());
```
> Collectors — это фабрика коллекторов: `toList()`, `toSet()`, `toMap()` и т.д.

---

### 6. forEach
Выполняет действие для каждого элемента.

```java
names.stream()
    .forEach(System.out::println);
```
> Терминальная операция — завершает поток.

---

### 7. count
Возвращает количество элементов.

```java
long count = nums.stream()
    .filter(n -> n % 2 == 0)
    .count();
```

---

### 8. reduce
Сворачивает поток в одно значение (сумму, произведение и т.д.).

```java
int sum = nums.stream()
    .reduce(0, (a, b) -> a + b);
```

---

### 9. anyMatch / allMatch / noneMatch
Проверяют логические условия для потока.

```java
boolean allPositive = nums.stream().allMatch(n -> n > 0);
boolean anyZero = nums.stream().anyMatch(n -> n == 0);
boolean noneNegative = nums.stream().noneMatch(n -> n < 0);
```

---

### 10. mapToInt / average / sum
Часто применяются к числам.

```java
double avg = nums.stream()
    .mapToInt(Integer::intValue)
    .average()
    .orElse(0.0);
```

---

## Короткие шпаргалочные лямбды

| Синтаксис | Что делает |
|------------|-------------|
| `n -> n * 2` | Умножает число на 2 |
| `s -> s.length()` | Возвращает длину строки |
| `s -> s.startsWith("A")` | Проверяет, начинается ли строка с "A" |
| `(a,b) -> a + b` | Складывает два значения |
| `String::toUpperCase` | Ссылка на метод — то же, что `s -> s.toUpperCase()` |
| `System.out::println` | Передаёт каждый элемент в println |

---

## Советы для запоминания

1. **filter → отбор**, **map → преобразование**, **collect → сбор**.  
2. Поток можно представить как конвейер: данные проходят через фильтры и преобразователи.  
3. Оригинальная коллекция не меняется.  
4. После терминальной операции (`forEach`, `collect`, `reduce`) поток закрывается.  
5. Всегда думай в терминах: “что я хочу получить, а не как это сделать”.

---

💬 Пример полного конвейера:
```java
List<String> result = users.stream()
    .filter(u -> u.length() > 3)
    .map(String::toUpperCase)
    .sorted()
    .distinct()
    .collect(Collectors.toList());
```
> Фильтрует, преобразует, сортирует, убирает дубли, собирает обратно в List.

